import { LyukShopValidators } from './lyuk-shop-validators';

describe('LyukShopValidators', () => {
  it('should create an instance', () => {
    expect(new LyukShopValidators()).toBeTruthy();
  });
});
